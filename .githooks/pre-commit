#!/bin/sh
# Honesty boundary — catch the not-yet-in-git traitor before git records it.
# Install once (committed via package.json "prepare"):  git config core.hooksPath .githooks
exec node scripts/precommit.ts
