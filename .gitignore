node_modules/
dist/
build/
*.log
.DS_Store
.env
.env.local
.vscode/
.idea/
.vitepress/cache
.vitepress/dist
.wrangler/
# The computed pages are now committed as SEALED EVIDENCE (regenerated each build by predocs:build,
# and snapshotted into git as timestamped evidence — computed AND stored as the record):
#   dashboard.md · boundaries.md · CHALLENGES.md · HEART.md · ACCOUNTING.md
public/greeting.json

# Lean build artefacts — oleans are rebuilt by scripts/lean.ts from the .lean sources
*.olean

# verification cache — a record of what the kernel said about exact bytes, rebuilt on demand
src/proof/.lean-cache.json

# agent worktrees — transient, removed when the agent finishes
.claude/worktrees/

# build analytics — recomputed on every build from artefacts; never carried between runs
src/analytics.json

# the readme trial's rendering — a verification artefact, not the published page
src/readme-trial.md
